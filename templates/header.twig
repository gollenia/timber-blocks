<header class="
	header alignfull 
	header--order-{{mobileParagraphPosition}} 
	header--content-{{paragraphOrientation}} 
	{{hasImage ? "" : "header--minimal"}} 
	{{ margin ? "mb-8" : ""}}"
>
	{{ content|raw}}
</header>
